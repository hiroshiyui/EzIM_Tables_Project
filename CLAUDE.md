# 輕鬆輸入法取碼規則

| 字數 | 取碼方式 | 碼數 |
| --- | --- | --- |
| 單一字元 | 第一字「頭」、「尾」 | 2 |
| 兩字詞 | 第一字「頭」、「尾」+ 第二字「頭」、「尾」 | 4 |
| 三字詞 | 第一字「頭」+ 第二字「頭」+ 第三字「頭」 | 3 |
| 四字詞 | 第一字「頭」+ 第二字「頭」+ 第三字「頭」+ 第四字「頭」 | 4 |
| 五字以上 | 第一字「頭」+ 第二字「頭」+ 第三字「頭」+ 第四字「頭」（最多取四碼） | 4 |

## 字元編碼挑選規則（決定該字元的「頭」、「尾」用哪一組編碼）

當一個字元在編碼表中對應到多組編碼時，依下列優先序挑選：

1. **單一按鍵（字根鍵）優先**：若有長度為 1 的編碼，優先採用該單鍵編碼。例：`一` 在 `["16", "m"]` 中採用 `m`。
2. **避開全數字編碼**：若無單鍵編碼，優先採用「非全數字」的編碼（全數字的多為速成碼，非標準字根碼）。例：`七` 在 `["76", "j'", "m'"]` 中跳過 `76`，採用 `j'`。
3. **依檔案排列順序**：若以上條件均無法決定，則採用編碼表中先出現的編碼。

範例：「八一七公報」(5 字以上，取前 4 字「頭」)
- 八 `["8", "86"]` → 採 `8` (單鍵) → 頭 `8`
- 一 `["16", "m"]` → 採 `m` (單鍵) → 頭 `m`
- 七 `["76", "j'", "m'"]` → 跳過全數字 `76`，採 `j'` → 頭 `j`
- 公 `["8/"]` → 採 `8/` → 頭 `8`
- 結果：`8mj8`
