# 輕鬆輸入法取碼規則

| 字數 | 取碼方式 | 碼數 |
| --- | --- | --- |
| 單一字元 | 第一字「頭」、「尾」 | 2 |
| 兩字詞 | 第一字「頭」、「尾」+ 第二字「頭」、「尾」 | 4 |
| 三字詞 | 第一字「頭」+ 第二字「頭」+ 第三字「頭」 | 3 |
| 四字詞 | 第一字「頭」+ 第二字「頭」+ 第三字「頭」+ 第四字「頭」 | 4 |
| 五字以上 | 第一字「頭」+ 第二字「頭」+ 第三字「頭」+ 第四字「頭」（最多取四碼） | 4 |

## 字元編碼挑選規則（決定該字元的「頭」、「尾」用哪一組編碼）

當一個字元在編碼表中對應到多組編碼時，依下列優先序挑選：

1. **單一按鍵（字根鍵）優先**：若有長度為 1 的編碼，優先採用該單鍵編碼。例：`一` 在 `["16", "m"]` 中採用 `m`。
2. **避開全數字編碼**：若無單鍵編碼，優先採用「非全數字」的編碼（全數字的多為速成碼，非標準字根碼）。例：`七` 在 `["76", "j'", "m'"]` 中跳過 `76`，採用 `j'`。
3. **依檔案排列順序**：若以上條件均無法決定，則採用編碼表中先出現的編碼。

範例：「八一七公報」(5 字以上，取前 4 字「頭」)
- 八 `["8", "86"]` → 採 `8` (單鍵) → 頭 `8`
- 一 `["16", "m"]` → 採 `m` (單鍵) → 頭 `m`
- 七 `["76", "j'", "m'"]` → 跳過全數字 `76`，採 `j'` → 頭 `j`
- 公 `["8/"]` → 採 `8/` → 頭 `8`
- 結果：`8mj8`

## 缺漏字元編碼推理規則

當編碼表內查無某字元時，依下列流程推理可能編碼，並可加入 `vendor/ezsource12-3/origtable/ez.orig-utf8.txt`：

### 推理流程

1. **拆解結構**：將字元拆為左右、上下、內外兩個主要部件（一頭一尾）。
2. **找同頭範本字**：找出與目標字共用「頭部件」的已知字（通常是同部首），取其頭碼。
3. **找同尾範本字**：找出與目標字共用「尾部件」（聲符或右半/下半）的已知字，取其尾碼。
4. **組合預測**：頭碼 + 尾碼。
5. **檢查重碼**：在編碼表中查詢預測碼，若同位置已有結構類似的字（同部首聚集區），則驗證預測合理。

### 多碼字元

- 範本字若有多組編碼（如雙頭碼或雙尾碼），目標字也應依相同規律補上多組。
- 例：佻 `["o'", "o-"]` 顯示 兆 部件支援 `'` 與 `-` 雙尾，故 䠷（身+兆）也應為 `,'` 與 `,-`。

### 異體字直接借用

- 若目標字有 Unicode 異體字（z-variant）且該異體字已有編碼，可直接沿用。
- 例：彞（U+5F5E）借用 彝（U+5F5D）的 `,t`、`.t`；皰（U+76B0）對 疱（U+75B1）。

### 實例彙整

| 目標字 | 結構 | 範本（頭） | 範本（尾） | 推理碼 |
|---|---|---|---|---|
| 礴 | 石+薄 | 磚 `m;` → `m` | 薄 `e;` → `;`、礡 `m;x` | `m;x` |
| 疱 | 疒+包 | 病 `yb`/`\`b` | 包 `,'` → `'` | `y'`、`\`'` |
| 酶 | 酉+每 | 醋 `ma` → `m` | 侮 `o=` → `=` | `m=` |
| 髴 | 髟+弗 | 髮 `mx` → `m` | 佛 `o'`/`on` | `m1`、`mn` |
| 肽 | 月+太 | 肥 `b'` → `b` | 汰 `ek` → `k` | `bk` |
| 䠷 | 身+兆 | 身 `,b` → `,` | 佻 `o'`/`o-` | `,'`、`,-` |
| 擀 | 扌+幹 | 握 `qg` → `q` | 幹 `jj` → `j` | `qj` |
| 焿 | 火+庚 | 烘 `f8` → `f` | 庚 `\`o` → `o` | `fo` |
| 彞 | 彐+羿 | 彝（異體字） | 彝（異體字） | `,t`、`.t`（沿用） |
| 吔 | 口+也 | 口 `r` | 池/地/他 `e'`/`g'`/`o'` → `'` | `r'` |
| 浜 | 氵+兵 | 海 `e=` → `e` | 兵 `,8` → `8` | `e8` |

### 特殊字元（無傳統部件結構）

對於無偏旁、無筆順的符號類字元（如 〇），需綜合下列來源：

1. **形似字**：找視覺相似字的編碼（〇 同 ○ 形 → `06`、`*`、`\`\``）
2. **義同字**：找意義相同字的編碼（〇 同 零 義 → `07`、`61`、`6y`）
3. **特殊字根**：依 EZ 自訂字根（如 `wx` 為「外包圍 + 罕用字字根」）

實例：〇 採用 `61`、`6y`、`wx`（綜合義同 + 特殊字根策略）。
